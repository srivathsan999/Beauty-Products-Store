// Reusable components (header, footer)
// Note: In a real project, these would be loaded via includes or a template engine
// For this vanilla JS project, we'll include them directly in each HTML file

